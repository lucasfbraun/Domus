APP_NAME=Domus
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
# Tunnel / public URL example:
# APP_URL=https://proprietario.code-fast.site
# APP_BASE_URL=https://proprietario.code-fast.site

# Docker (Sail) — descomente/ajuste se for rodar via docker compose
# APP_PORT=8000
# VITE_PORT=5173
# FORWARD_REDIS_PORT=6379

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
MEDIA_DISK=public
QUEUE_CONNECTION=database

# Moeda/formatacao usada no backend e compartilhada com o frontend
# (App\Support\Money). Padroes ja sao os corretos pra este projeto (BRL).
# MONEY_CURRENCY=BRL
# MONEY_LOCALE=pt-BR
# MONEY_DECIMALS=2
# MONEY_SYMBOL="R$"

# Botao "Rodar suite de testes" em Admin > Funcionalidades. Padrao: so liga
# quando APP_ENV=local (producao nao tem o Pest instalado). Normalmente nao
# precisa mexer aqui.
# FEATURE_CHECKS_ENABLED=true

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
# No Docker (Sail), use o nome do servico: REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Brevo (SMTP) — produção / envio real
# MAIL_MAILER=smtp
# MAIL_SCHEME=null
# MAIL_HOST=smtp-relay.brevo.com
# MAIL_PORT=587
# MAIL_USERNAME=seu-login@smtp-brevo.com
# MAIL_PASSWORD=
# MAIL_FROM_ADDRESS="noreply@seudominio.com"
# MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Mercado Pago (Orders API + OAuth por recebedor)
# Producao: MP_CLIENT_ID + MP_CLIENT_SECRET; cada recebedor conecta via OAuth no admin.
# Local/testes: MP_ACCESS_TOKEN opcional como atalho sem OAuth do recebedor.
MP_CLIENT_ID=
MP_CLIENT_SECRET=
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=
MP_WEBHOOK_SECRET=
MP_SANDBOX_CONNECT=true

# E-mail: local usa MAIL_MAILER=log (ou Mailpit). Produção: Brevo SMTP (ver bloco comentado acima).

# WAHA WhatsApp
WAHA_BASE_URL=
WAHA_API_KEY=
WAHA_SESSION=default

APP_BASE_URL="${APP_URL}"
